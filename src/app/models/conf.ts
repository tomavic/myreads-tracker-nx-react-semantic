export const MAX_RESULTS = 50;

export const PATHS = {
  list: '/',
  bookDetails: '/book',
  search: '/search',
};

export const DND = {
  type: 'book',
};

export const DEFAULT_BOOK_COVER = 'book.png';
